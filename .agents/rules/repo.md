# Repo Rules

Follow `../../AGENTS.md` for canonical repo instructions.

Before editing, inspect the relevant files and run `git status --short`. Preserve unrelated user changes, keep diffs small, and use only commands that are documented in the repo or directly discoverable from its manifests.
