# Commands

Use the commands documented in `../../AGENTS.md` and `../../README.md`.

Add command notes here only when a repeated workflow needs more detail than the root guide can hold. Do not add invented commands; derive them from manifests, workflow files, or existing documentation.
